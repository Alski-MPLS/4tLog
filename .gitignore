# macOS
.DS_Store

# Notes / planning docs not meant for the repo
plan.md

# Ansible
*.retry
ansible/output/

# Ansible Vault password files / accidental decrypted copies
# (my-vault.yml itself is fine to commit - it's vault-encrypted)
.vault_pass
.vault-password
vault_pass.txt
*.decrypted

# Python / venv
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/

# Application config / secrets
.env
certs/
users.json
groups.json
faz_targets.json
